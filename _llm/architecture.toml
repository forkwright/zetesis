[meta]
name = "zetesis"
repo = "forkwright/zetesis"
generated = false
source_docs = [
  "README.md",
  "CLAUDE.md",
  "Cargo.toml",
  "docs/design/contract-baseline.md",
]

[architecture]
summary = "Pre-release sovereign research substrate. Landed: the sylloge typed surface (provider and deep-research traits, constraints with a fail-closed network-target check, citations, results, cost and in-memory budget arithmetic, in-memory deep-research lifecycle with an offline fixture) plus StaticAcquirer for bounded static acquisition with evidence envelope v1, re-exported by the zetesis facade. Planned: provider adapters and routing, durable reservation ledger, cache, deep inquiry through a consumer-granted model contract, steelmanning, and briefings."
layers = [
  "facade",
  "provider-routing",
  "static-acquisition",
  "budget-ledger",
  "cache",
  "deep-inquiry",
  "steelman",
  "briefing",
  "consumer-adapters",
]

[[architecture.crates]]
name = "zetesis"
path = "crates/zetesis"
role = "Landed: facade re-exporting the sylloge surface, with steelman and briefing aliases. Planned: CLI, daemon binary, and consumer adapter wiring."

[[architecture.crates]]
name = "sylloge"
path = "crates/sylloge"
role = "Landed: Provider and DeepResearch traits; SearchConstraints with the network-target check and non-forgeable ValidatedTarget; StaticAcquirer with per-hop validation, the Connector seam, bounded gzip/deflate decoding, static text extraction, and evidence envelope v1 with fingerprint and replay (Crawler and PageContent retired); citation, result, freshness, cost, and budget types; in-memory LocalDeepResearch with an offline fixture. Planned: Tier-0 adapters, routing, durable ledger, cache, deep-inquiry loop."

[[architecture.crates]]
name = "elenkhos"
path = "crates/elenkhos"
role = "Landed: reserved crate boundary (marker type). Planned: retrospective steel-manning engine."

[[architecture.crates]]
name = "synopsis"
path = "crates/synopsis"
role = "Landed: reserved crate boundary (marker type). Planned: briefing synthesizer."

[[interfaces]]
name = "zetesis facade crate"
kind = "library"
path = "crates/zetesis"
status = "landed"

[[interfaces]]
name = "zetesis daemon"
kind = "cli"
path = "crates/zetesis"
status = "planned"
