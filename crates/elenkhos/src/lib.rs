#![doc = "Retrospective steel-manning engine scaffold for zetesis."]
#![deny(missing_docs)]
#![forbid(unsafe_code)]

/// Marker type for the locked `elenkhos` crate boundary.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Elenkhos;
