name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: release-please-${{ github.ref }}
  cancel-in-progress: true

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Release pipeline pending first implementation crate
        run: echo "Release automation activates when Phase 01 adds workspace packages."
